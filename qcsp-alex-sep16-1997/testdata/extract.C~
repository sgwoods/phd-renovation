#include <iostream.h>

const int MAX = 50;

main(int argc, char* argv[])
{
  const int factor = 5;
  const int level = 5;
  int data[level][MAX];
  double avg[level];
  int count = 0;

  int tempint;
  double tempdouble;

  ifstream infile(argv[1], ios::in);
  ofstream outfile(argv[2], ios::out);

  

}
