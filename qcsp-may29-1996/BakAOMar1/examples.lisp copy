(defun test ()

  (let (
        (x '(apples peaches pumpkin pie))
        )

    (dolist (z x)

      (when (eq 'pumpkin z)
        (setf (car x) 'fred ))

      (format t " ~A "  z)

      )))
