;; ***************************************************************************
;; quilici determines initial variable only from template itself
;;  as an expert-determined key.  Note that the remainder of the
;;  variables are unordered - selected according to either (1)
;;  the partial ordering of constraints from the first variable, or
;;  in absence of control info, randomly I suppose.      
;;
;; called in adt-simple.lisp
;;
(defun quilici-sort (init-var-set ... ))

;; ***************************************************************************
;; quilici search redefines backtracking so as to apply constraints in
;; the order specified while orderings exists.
;;
;; called in adt-simple.lisp
;;
(defun quilici-search ))

;; note consistent-p in adt-simple.lisp may need to be changed.  This 
;;  unifies any constraints between two slots as a single constraint
;;  application function and calls them together.  It takes as parameters
;;  slot1, value1  slot2, value2  a partial solution (not needed)
;;  and a KEY for sorting constraints ... here we can use a specific flag
;;  to indicate that certain constraint types should be applied first/last
;;  etc and create a sorting function  within the function "sort-constr"
;;  which does this.  Currently sort-constr either uses random or else
;;  the GIVEN order.  

