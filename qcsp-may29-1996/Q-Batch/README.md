# Q-Batch (Preserved Duplicate)

This tree has been reviewed and is treated as a **preserved duplicate** of the
canonical ACL-era baseline under:

- `/Users/stevenwoods/phd-renovation/data/acl-experiments/`

## What This Means

- Keep this copy for **snapshot integrity** inside the May 1996 tree.
- Do **not** treat it as the active source of truth for the supported thesis
  validation pipeline.
- When validating or extending the main project, prefer
  `data/acl-experiments/` and the modern comparison path in `Q-Batch-SBCL/`.

## Why It Still Exists

This directory preserves the historical local layout of the May 1996 snapshot.
That provenance still matters, even though the main project now uses the
consolidated `data/acl-experiments/` copy as the canonical baseline.
